# Slide 4: Discussion Setup

Generate one full-slide 16:9 presentation image.

## Exact Title

`Discussion Setup`

## Exact Subtitle

`What has to be true for this to work in real care?`

## Layout

Six compact question tiles in a two-by-three grid. Each tile has a bold category label and one concrete question.

Tile text:

1. `Adoption`
   `Who has to move first: apps, EHRs, clinics, payers, or patients?`
2. `Workflow`
   `Where does patient-shared data land so it helps instead of creating noise?`
3. `Trust`
   `What should be labeled, reviewed, or promoted to active lists?`
4. `Patient value`
   `What makes patients come back to the app before the next visit?`
5. `AI`
   `Can AI summarize without hiding provenance or consent?`
6. `Next protocol`
   `What has to be true before visit-specific check-in can work?`

Use a subtle background loop or pathway around the tiles: app gets better -> patients return -> EHR uses information -> workflow improves -> app gets better. Keep it understated.

## Footer

Small footer reference only: `KTC spec: ktc-spec.github.io`

## Avoid

- Do not show panelist headshots or biographies.
- Do not include instructions like "keep answers concrete".
- Do not use generic slogans.
