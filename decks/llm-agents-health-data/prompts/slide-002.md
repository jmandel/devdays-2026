# Slide 2 Prompt

Title: `The Smallest Useful Architecture`

Show two separated lanes: `access rail` and `reasoning loop`. Access produces local files from `SMART/FHIR connect` or `EHI request package`. Reasoning loops through `read SKILL.md`, `inspect files`, `write scripts`, `verify`, and `cite evidence`. Output is `answer + next questions`.
