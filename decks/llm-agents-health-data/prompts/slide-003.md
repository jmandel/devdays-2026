# Slide 3 Prompt

Title: `A Skill Is Executable Documentation`

Show a `skill bundle` folder containing `SKILL.md`, `scripts/`, `references/`, `examples/`, and `guardrails`. Include a smaller execution-surface card labeled `CLI tools + scripts` with descriptor `called via shell or curl`, connected to `scripts/`. Also show small chips `bun`, `curl`, and `jq`. Takeaway: the audience can read and change the operating procedure.
