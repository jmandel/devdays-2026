# Slide 7 Prompt

Title: `Guardrail: Side Effects Need An Explicit Gate`

Show workflow zones: `drafting` with `generate PDF`, `render preview`, `check recipient`, `check page count`; center gate `user approves`; `acting` with `fax`, `email`, `submit`, `upload`. Caution chip: `do not run before approval`; command chip after gate: `send-fax.ts`.
