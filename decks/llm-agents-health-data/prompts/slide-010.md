# Slide 10 Prompt

Title: `Preview Skill: Build An EHI Deep Dive`

Show build pipeline: evidence (`SQLite`, `TSV`, `notes/media`) -> `STORYBOARD.md` -> `viewmodel.json` -> `BUILD.md`, plus `parts/` and `scripts/` feeding a `static deep-dive app`. Include loop label `EXTRACT -> ENRICH -> SYNTHESIZE` and caption `clean projection with evidence`.
