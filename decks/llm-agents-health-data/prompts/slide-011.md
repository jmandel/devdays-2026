# Slide 11 Prompt

Title: `Pick A Build Lane`

Four equal cards: `Connect` with `health-skillz`; `Request` with `request-my-ehi`; `Analyze` with `q.ts --table`; `Extend` with `SKILL.md`. Note: `Choose the lane that matches your data and comfort level.`
