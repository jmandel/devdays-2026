import React, { useEffect, useMemo, useState } from "react";
import { createRoot } from "react-dom/client";
import { useQaStore, type PublicQuestion, type ThemeQuestion } from "./stores/qaStore";

function sidFromPath() { const parts = location.pathname.split("/").filter(Boolean); const qa = parts.indexOf("qa"); return qa > 0 ? parts[qa - 1] : (parts.at(-1) || ""); }
function fmt(ts?: number) { return ts ? new Date(ts * 1000).toLocaleTimeString() : ""; }
function Status({ value }: { value: string }) { return <span className={`pill ${value}`}>{value}</span>; }


function AdminDashboard() {
  const { adminSessions, loadAdminSessions, createSession } = useQaStore();
  const [msg, setMsg] = useState("");
  const [title, setTitle] = useState("");
  const [presenter, setPresenter] = useState("");
  const [description, setDescription] = useState("");
  useEffect(() => { loadAdminSessions().catch(e => setMsg((e as Error).message)); }, []);
  if (msg === "unauthorized") return <main className="shell"><section className="card"><div className="eyebrow">Admin locked</div><h1>Operator key required</h1><p className="muted">Unlock the admin console to manage talks.</p><a className="btn primary" href="/admin/login-page">Open login</a></section></main>;
  async function submit(e: React.FormEvent) {
    e.preventDefault();
    if (!title.trim()) return;
    try { const id = await createSession({ title, presenter, description }); location.href = `/admin/talks/${id}`; }
    catch (e) { setMsg((e as Error).message); }
  }
  const totals = adminSessions?.totals;
  return <main className="shell"><div className="top"><div><div className="brand">DevDays Feedback</div><h1>Admin dashboard</h1></div><form method="POST" action="/logout"><button className="btn">logout</button></form></div>{msg && <div className="notice">{msg}</div>}<section className="grid"><div className="card"><div className="eyebrow">Overview</div><h2>{totals?.sessions ?? 0} talks</h2><p className="muted">{totals?.active ?? 0} active · {totals?.feedback ?? 0} feedback responses</p></div><form className="card" onSubmit={submit}><div className="eyebrow">Create talk</div><h2>New room</h2><input value={title} onChange={e=>setTitle(e.target.value)} placeholder="Title" required/><input style={{marginTop:10}} value={presenter} onChange={e=>setPresenter(e.target.value)} placeholder="Presenter"/><textarea style={{marginTop:10}} value={description} onChange={e=>setDescription(e.target.value)} placeholder="Description / room context"/><button className="btn primary" style={{marginTop:10,width:"100%"}}>Create</button></form></section><section className="card"><div className="eyebrow">Talks</div><h2>Rooms</h2>{adminSessions?.sessions.length ? adminSessions.sessions.map(s => <div className="q" key={s.id}><div className="spread"><div><strong>{s.title}</strong><p className="muted">{s.presenter} · {s.feedback_count} feedback</p></div><Status value={s.qa_state}/></div><div className="row"><a className="btn primary" href={`/admin/talks/${s.id}`}>control room</a><a className="btn outline" href={`/t/${s.id}`} target="_blank">attendee</a><a className="btn" href={`/admin/talks/${s.id}/qr`}>QR</a></div></div>) : <p className="muted">No talks yet.</p>}</section></main>;
}

function LoginPage() {
  return <main className="shell"><section className="card" style={{maxWidth:520, margin:"64px auto"}}><div className="eyebrow">Admin console locked</div><h1>Enter operator key</h1><form method="POST" action="/admin/login"><input type="password" name="key" autoComplete="current-password" autoFocus required/><button className="btn primary" style={{width:"100%", marginTop:12}}>unlock console</button></form></section></main>;
}

function SlidesPage() {
  const id = sidFromPath();
  const { talk, slidesPayload, publicPayload, connection, loadTalk, loadSlides, loadPublic, connect, disconnect } = useQaStore();
  const [mode, setMode] = useState<"themes" | "raw">("themes");
  useEffect(() => { loadTalk(id); loadSlides(id).catch(()=>{}); loadPublic(id).catch(()=>{}); connect(id); const t = setInterval(()=>{ loadSlides(id).catch(()=>{}); loadPublic(id).catch(()=>{}); }, 10000); return () => { clearInterval(t); disconnect(); }; }, [id]);
  const themes = slidesPayload?.questions || [];
  const raw = publicPayload?.questions || [];
  return <main className="shell projector" style={{maxWidth:1100}}><div className="top"><div><div className="eyebrow">Projector Q&A</div><h1>{talk?.title || id}</h1></div><Status value={connection}/></div><section className="card"><div className="spread"><div><h2>{mode === "themes" ? "Synthesized presenter themes" : "Raw attendee questions"}</h2><p className="muted">Themes are presenter-ready summaries synthesized from audience submissions.</p></div><div className="segmented" role="group" aria-label="Projector Q&A mode"><button type="button" aria-pressed={mode === "themes"} onClick={()=>setMode("themes")}>Themes</button><button type="button" aria-pressed={mode === "raw"} onClick={()=>setMode("raw")}>Raw questions</button></div></div>{mode === "themes" ? <div className="theme-grid projector-themes">{themes.length ? themes.slice(0,5).map((q,i)=><div className="theme-card" key={q.id}><div className="muted">Theme {i+1} · {q.source_count ?? 1} source{(q.source_count ?? 1) === 1 ? "" : "s"} · score {q.support_count}</div><strong>{q.text}</strong></div>) : <p className="muted">No synthesized themes yet.</p>}</div> : <div>{raw.length ? raw.slice(0,8).map((q,i)=><div className="q secondary" key={q.id}><div className="muted">#{i+1} · score {q.support_count}</div><strong>{q.text}</strong></div>) : <p className="muted">No raw questions yet.</p>}</div>}</section></main>;
}

function AttendeePage() {
  const id = sidFromPath();
  const { talk, publicPayload, connection, loadTalk, loadPublic, connect, disconnect, submitQuestion, vote, feedback, pulse, error } = useQaStore();
  const [question, setQuestion] = useState("");
  const [status, setStatus] = useState("");
  const [pulseStatus, setPulseStatus] = useState("");
  const [tags, setTags] = useState<string[]>([]);
  const [comment, setComment] = useState("");
  const [rating, setRating] = useState("");
  useEffect(() => { loadTalk(id); loadPublic(id).catch(()=>{}); connect(id); return disconnect; }, [id]);
  const qaOpen = publicPayload?.session.qa_state === "open" || talk?.qa_state === "open";
  const qs = publicPayload?.questions || [];
  const speakerName = talk?.presenter?.trim() || "the speaker";
  const pulses = ["I’m with you", "I’m confused", "Too fast", "Too slow"];
  const fbTags = ["demos", "architecture", "code", "real-world examples", "tradeoffs", "Q&A time", "resources"];
  async function sendPulse(value: string) { setPulseStatus("sending…"); try { await pulse(id, value); setPulseStatus(`sent: ${value}`); } catch(e) { setPulseStatus((e as Error).message); } }
  async function onSubmit(e: React.FormEvent) { e.preventDefault(); if (!question.trim()) return; setStatus("submitting…"); try { await submitQuestion(id, question); setQuestion(""); setStatus("queued"); } catch(e) { setStatus((e as Error).message); } }
  async function onFeedback(e: React.FormEvent) { e.preventDefault(); setStatus("sending feedback…"); try { await feedback(id, { rating: rating ? Number(rating) : null, comment, tags }); setComment(""); setTags([]); setRating(""); setStatus("feedback sent"); } catch(e) { setStatus((e as Error).message); } }
  return <main className="phone">
    <div className="top"><div className="brand">DevDays Feedback</div><Status value={connection}/></div>
    {error && <div className="notice">{error}</div>}
    <section className="card"><div className="eyebrow">Talk</div><h1>{talk?.title || id}</h1>{talk?.presenter && <p className="muted">{talk.presenter}</p>}{talk?.description && <p>{talk.description}</p>}{talk?.slides_url ? <a className="btn primary" href={talk.slides_url} target="_blank">Open slides</a> : <p className="muted">Slides link coming soon.</p>}</section>
    <section className="card" aria-labelledby="pulse-heading"><div className="eyebrow">Live pulse check</div><h2 id="pulse-heading">How is this landing right now?</h2><p className="muted">Tap any time. No text required. Private to the presenter/organizer.</p><div className="choice-grid">{pulses.map(v => <button type="button" key={v} className="chip tap" onClick={() => sendPulse(v)}>{v}</button>)}</div>{pulseStatus && <p className="muted" role="status">{pulseStatus}</p>}</section>
    <section className="card"><div className="spread"><div><div className="eyebrow">Public Q&A</div><h2>Ask a public question</h2></div><Status value={qaOpen ? "open" : "closed"}/></div>{qaOpen ? <form onSubmit={onSubmit}><label htmlFor="questionText">Public Q&A question for {speakerName}</label><textarea id="questionText" value={question} onChange={e=>setQuestion(e.target.value)} maxLength={1000} placeholder="Ask a concise question for the presenter…"/><button className="btn primary" style={{width:"100%",marginTop:8}}>Submit question</button></form> : <p className="muted">Questions are closed right now.</p>}{status && <p className="muted" role="status">{status}</p>}<h3>Questions from the room</h3>{qs.length ? qs.map(q => <PublicQuestionRow key={q.id} q={q} onVote={(v)=>vote(id,q.id,v).catch(e=>setStatus((e as Error).message))}/>) : <p className="muted">No questions yet.</p>}</section>
    <form className="card" onSubmit={onFeedback} aria-labelledby="feedback-heading"><div className="eyebrow">Private feedback to presenter</div><h2 id="feedback-heading">Private feedback to {speakerName}</h2><p className="muted">Private to the presenter/organizer, separate from Public Q&A.</p><fieldset><legend>How useful was this session for you?</legend><div className="rating-scale">{[[1,"Not useful"],[2,"Slightly useful"],[3,"Moderately useful"],[4,"Very useful"],[5,"Extremely useful"]].map(([value,label])=><label key={value} className={`rating-option ${rating===String(value)?"selected":""}`}><input type="radio" name="rating" value={value} checked={rating===String(value)} onChange={e=>setRating(e.target.value)}/><span>{value}</span><small>{label}</small></label>)}</div></fieldset><label htmlFor="feedbackComment">What is one thing {speakerName} should keep, change, or clarify?</label><textarea id="feedbackComment" value={comment} onChange={e=>setComment(e.target.value)} placeholder="Optional"/><fieldset><legend>Optional topics</legend><div className="row">{fbTags.map(t=><button type="button" key={t} className={`chip ${tags.includes(t)?"selected":""}`} aria-pressed={tags.includes(t)} onClick={()=>setTags(tags.includes(t)?tags.filter(x=>x!==t):[...tags,t])}>{t}</button>)}</div></fieldset><button className="btn primary" style={{width:"100%",marginTop:8}}>Send feedback</button></form>
  </main>;
}
function PublicQuestionRow({ q, onVote }: { q: PublicQuestion; onVote: (v: 1|-1)=>void }) { return <div className="q"><div className="spread"><strong>{q.text}</strong><Status value={q.answered ? "answered" : q.status}/></div><div className="meta"><span>score={q.support_count}</span><span>{fmt(q.created_at)}</span>{q.theme_id && <span>group={q.theme_id}</span>}{!q.answered && <><button className="btn outline" onClick={()=>onVote(1)}>▲</button><button className="btn outline" onClick={()=>onVote(-1)}>▼</button></>}</div></div>; }

function AdminPage() {
  const id = sidFromPath();
  const { talk, presenterPayload, feedbackSummary, connection, loadTalk, loadPresenter, loadFeedbackSummary, connect, disconnect, setQaState, action, runAgent, error } = useQaStore();
  const [msg, setMsg] = useState("");
  useEffect(() => { loadTalk(id); loadPresenter(id).catch(e=>setMsg((e as Error).message)); loadFeedbackSummary(id).catch(()=>{}); connect(id); const t = setInterval(()=>loadFeedbackSummary(id).catch(()=>{}), 10000); return () => { clearInterval(t); disconnect(); }; }, [id]);
  const themes = useMemo(() => presenterPayload?.themes || presenterPayload?.questions || [], [presenterPayload]);
  const qaState = presenterPayload?.session.qa_state || talk?.qa_state || "unknown";
  const pulseOptions = ["I’m with you", "I’m confused", "Too fast", "Too slow"];
  const ratingLabels: Record<string, string> = { "1": "Not useful", "2": "Slightly useful", "3": "Moderately useful", "4": "Very useful", "5": "Extremely useful" };
  async function call(p: Promise<void>) { try { setMsg("working…"); await p; await loadFeedbackSummary(id).catch(()=>{}); setMsg("updated"); } catch(e) { setMsg((e as Error).message); } }
  if (msg === "unauthorized" || error === "unauthorized") return <main className="shell"><section className="card"><div className="eyebrow">Room console locked</div><h1>Operator access required</h1><p className="muted">Use the room capability link or unlock the admin console first.</p><a className="btn primary" href="/admin">Open admin login</a></section></main>;
  return <main className="shell"><div className="top"><div><div className="brand">DevDays Control Room</div><h1>{talk?.title || id}</h1><p className="muted">{talk?.presenter}</p></div><Status value={connection}/></div>{error && <div className="notice">{error}</div>}{msg && <div className="notice">{msg}</div>}<section className="card"><div className="spread"><div><div className="eyebrow">Q&A state</div><h2>{qaState}</h2></div><div className="row"><button className="btn success" onClick={()=>call(setQaState(id,"open"))}>Accept questions</button><button className="btn warn" onClick={()=>call(setQaState(id,"paused"))}>Pause questions</button><button className="btn danger" onClick={()=>call(setQaState(id,"closed"))}>Close questions</button><button className="btn outline" onClick={()=>call(runAgent(id))}>Synthesize themes</button><a className="btn quiet" href={`/admin/talks/${id}/ai-run`}>AI processing log</a><a className="btn quiet" href={`/t/${id}`} target="_blank">Open public page</a><a className="btn quiet" href={`/slides/t/${id}/qa`} target="_blank">Open projector Q&A</a><a className="btn quiet" href={`/admin/talks/${id}/export`}>Export CSV</a></div></div></section><section className="card"><div className="eyebrow">Feedback map</div><h2>Live pulse and session feedback</h2><p className="muted">Private to the presenter/organizer.</p><h3>Recent pulse (last 5 minutes)</h3>{feedbackSummary ? <div>{pulseOptions.map(option => { const count = feedbackSummary.pulse.counts[option] ?? 0; const pct = feedbackSummary.pulse.total ? Math.round((count / feedbackSummary.pulse.total) * 100) : 0; return <div className="metric-row" key={option}><strong>{option}</strong><div className="bar" aria-label={`${option}: ${count}`}><span style={{width:`${pct}%`}} /></div><span>{count}</span></div>; })}</div> : <p className="muted">No pulse data yet.</p>}<h3>Session usefulness</h3>{feedbackSummary ? <div>{["5","4","3","2","1"].map(value => { const count = feedbackSummary.session_feedback.rating_distribution[value] ?? 0; const max = Math.max(1, ...Object.values(feedbackSummary.session_feedback.rating_distribution)); return <div className="metric-row" key={value}><strong>{value} · {ratingLabels[value]}</strong><div className="bar" aria-label={`${ratingLabels[value]}: ${count}`}><span style={{width:`${Math.round((count / max) * 100)}%`}} /></div><span>{count}</span></div>; })}</div> : null}<h3>Comments</h3>{feedbackSummary?.session_feedback.comments.length ? feedbackSummary.session_feedback.comments.map(c => <div className="q" key={c.id}><div className="meta"><span>{fmt(c.submitted_at)}</span>{c.rating && <span>{c.rating}/5</span>}{c.tags.map(t => <span className="chip" key={t}>{t}</span>)}</div>{c.comment && <p>{c.comment}</p>}</div>) : <p className="muted">No session feedback comments yet.</p>}</section><section className="card"><div className="eyebrow">Run Q&A</div><h2>Synthesized presenter themes</h2><p className="muted">Top presenter-ready themes synthesized from audience questions. Raw submissions stay secondary.</p>{themes.length ? themes.map(q => <ThemeRow key={q.id} q={q} onAction={(a)=>call(action(id,q.id,a))}/>) : <p className="muted">No live themes yet. Submit audience questions or synthesize themes.</p>}</section></main>;
}
function ThemeRow({ q, onAction }: { q: ThemeQuestion; onAction: (a:string)=>void }) { return <div className="q"><div className="spread"><div><strong>{q.text}</strong><div className="meta"><span>score {q.support_count}</span><span>{q.source_count ?? "?"} source{(q.source_count ?? 0) === 1 ? "" : "s"}</span><span>priority={q.priority}</span><span>{fmt(q.created_at)}</span></div></div><Status value={q.status}/></div><div className="row" style={{marginTop:10}}>{q.status !== "pinned" ? <button className="btn outline" onClick={()=>onAction("pin")}>pin</button> : <button className="btn outline" onClick={()=>onAction("unpin")}>unpin</button>}<button className="btn success" onClick={()=>onAction("answer")}>answered</button><button className="btn danger" onClick={()=>onAction("hide")}>hide</button>{["hidden","answered"].includes(q.status) && <button className="btn" onClick={()=>onAction("restore")}>restore</button>}</div></div>; }
function App() { const path = location.pathname; if (path === "/admin/login-page") return <LoginPage/>; if (path === "/admin" || path === "/admin/dashboard") return <AdminDashboard/>; if (path.startsWith("/admin/talks/")) return <AdminPage/>; if (path.startsWith("/slides/t/") || path.startsWith("/slides/s/") || path.startsWith("/embed/t/") || path.startsWith("/embed/s/")) return <SlidesPage/>; if (path.startsWith("/t/")) return <AttendeePage/>; return <main className="shell"><h1>DevDays Feedback</h1><p>Unsupported React route.</p></main>; }

createRoot(document.getElementById("root")!).render(<App />);
