# Slide 1 Prompt

Title: `Toward Conversational Interop`

Subtitle: `Agents structuring the long tail on the fly`

Show two organizational agents across a boundary: clinical liaison agent on the left with `FHIR record` and `local tools`; payer/registry/trial agent on the right with `private rules`. Between them: short transcript turns becoming a structured `evidence package` and `audit transcript`. Include small chip `Banterop: banterop.fhir.me`.
