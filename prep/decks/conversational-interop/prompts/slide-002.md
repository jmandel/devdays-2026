# Slide 2 Prompt

Title: `Not LLMs Instead Of Standards`

Show standards rails as a foundation: `FHIR / SMART`, `CDS Hooks`, `Da Vinci`, `X12`. Above the rails, a conversation layer labeled `late-bound negotiation`. Output card: `structured artifact when needed`. Message: standards remain the rails; COIN handles the negotiated long tail.
