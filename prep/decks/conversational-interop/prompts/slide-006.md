# Slide 6 Prompt

Title: `Late-Bound Structure`

Show three transcript bubbles transforming into a JSON-like `evidence package`. Include source chips: `FHIR reference`, `note quote`, `human answer`, `policy criterion`. Output includes `provenance receipt`. Message: `the shape emerges from the task, then becomes structured`.
