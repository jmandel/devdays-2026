# Slide 8 Prompt

Title: `Design For Delay`

Show a liaison task UX: EHR order row with quiet status `working`, louder amber status `needs input`, task panel with `transcript`, `artifacts`, `focused question`, and final state `completed`. Include chip `FHIR Task` and states `working`, `input-required`, `completed`.
