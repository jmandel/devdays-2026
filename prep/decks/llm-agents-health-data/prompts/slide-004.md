# Slide 4 Prompt

Title: `Project 1: Health Skillz Connects To Patient Portals`

Show the accurate flow: `patient` -> `portal login` -> `SMART authorization` with `patient/*.rs` and `launch context` -> `FHIR API access` with `access token` -> `Health Skillz` -> `local files` with `health-data/*.json`, `DocumentReference`, `attachments[]` -> `cited answer`. Include small callout `Epic sandbox: fhircamila / epicepic1`.
